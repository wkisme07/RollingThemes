<div id="box">
  <h1>Rolling Themes</h1>
  <div id="block-login" class="block">
    <h2>Forgot your password?</h2>

    <div class="content login">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end %>
      </div>

      <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :class => 'form login' }) do |f| %>
        <div class="flash">
          <% unless resource.errors.blank? %>
            <div class="message warning">
              <ul class="user-form-error">
                <% resource.errors.full_messages.each do |error| %>
                  <li><%= error %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
        </div>

        <div class="group wat-cf">
          <div class="left">
            <%= f.label :email, :class => 'label right' %>
          </div>
          <div class="right">
            <%= f.email_field :email, :class => 'text_field' %>
          </div>
        </div>

        <div class="group navform wat-cf">
          <div class="right">
            <button type="submit" class="button">
              <%= image_tag "key.png", :alt => '-' %> Send me reset password instructions
            </button>
          </div>
        </div>
        <div class="group navform wat-cf">
          <div class="right">
            <%= render "links" %>
          </div>
        </div>
      <% end %>

    </div>
  </div>
</div>

<%= content_for :stylesheet do %>
  <%= stylesheet_link_tag "web_app_theme" %>
<% end %>